// import { 
//     SET_SYSTEM_DIALOG,
// } from '../mutation-types'

// state
const state = {
    // loading: {
    //     show: false  // 是否顯示
    // }
}

// getters
const getters = {
    
}

// mutations
const mutations = {
    // 設定 loading 圖
    // [SET_SYSTEM_LOADING] (state, bool) {
    //     state.loading.show = bool  //是否顯示
    // },
}

// actions
const actions = {
    // 改變 dialog 內容
    // chDialog ({ commit }, payload) {
    //     commit('SET_SYSTEM_DIALOG', payload)
    // },
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}